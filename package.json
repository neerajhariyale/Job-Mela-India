{
  "name": "job-mela-india",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "echo \"No build step required\"",
    "start": "cd jobportal-backend && node server.js",
    "install:backend": "cd jobportal-backend && npm install",
    "install:frontend": "cd jobportal-frontend && npm install"
  },
  "workspaces": [
    "jobportal-backend",
    "jobportal-frontend"
  ]
}

